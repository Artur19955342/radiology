VITE_DATA_BACKEND=local

# Set VITE_DATA_BACKEND=api when deploying with Vercel Functions.
POSTGRES_URL=

# Server-only. Do not expose this value with a VITE_ prefix.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash
